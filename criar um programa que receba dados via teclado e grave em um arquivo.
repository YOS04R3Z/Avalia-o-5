 #finclude <stdio.h>
 #didefine TAMANHO 100

 intmain()
{

 char texto[TAMANHO];
E FILE* fp;

printf("Digite o texto que deseja gravar num arquivo.n");
fgets(texto, TAMANHO, stdin);

if((fp = fopen("arquivo.txt", "w"))==NULL)
{
printf("O arquivo não pode ser aberto.Nn");
” exit(1);
}
fputs(texto, fp);
felose(fp);

return 0;
}
